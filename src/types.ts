export type User = {
  id: string;
  name: string;
  email: string;
  image: string;
};

export type Competition = {
  id: string;
  name: string;
  image: string;
  description: string;
  startDate: string;
  endDate: string;
};